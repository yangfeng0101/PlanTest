## Codebase Patterns
- Use Zustand with persist middleware for state that should survive page reloads
- Backend RBAC permissions are defined as "resource:action" format (e.g., "device:write")
- Frontend permission checks use the same "resource:action" format for consistency
- Login page uses gradient background with card overlay pattern
- Device groups use in-memory storage (dict) - in production, use database
- Group routes follow RESTful pattern: GET /groups, POST /groups, PUT /groups/{id}, DELETE /groups/{id}
- Statistics service uses in-memory lists for session/execution data - production needs database
- TrendChart component supports multiple chart types: line, bar, area, histogram
- Alert service supports Feishu/DingTalk webhook notifications via httpx async client
- Alert rules use cooldown_seconds to prevent notification spam
- OCR service uses PaddleOCR primary, Tesseract fallback, lazy initialization on first use

---

## 2026-04-10 - STATS-001
- Created statistics service for device usage and task execution tracking
- Implemented time-series trend analysis with configurable granularity
- Added report generation (daily, weekly, monthly)
- Statistics stored in-memory (production would use database)
- **Files changed:**
  - services/report-svc/app/services/statistics.py (new)
  - services/report-svc/app/api/statistics.py (new)
  - services/report-svc/app/api/__init__.py (updated exports)
  - services/report-svc/app/main.py (added statistics router)
- **Learnings for future iterations:**
  - Statistics service provides record_session() and record_task() for manual data entry
  - TimeGranularity enum: HOURLY, DAILY, WEEKLY, MONTHLY for trend analysis
  - Reports include device_usage, task_stats, top_devices, top_users, and usage_trend
---

## 2026-04-10 - GROUP-001
- Created DeviceGroup model with GroupType enum (custom, system, tag)
- Implemented groups API routes with full CRUD operations
- Added device membership management (add/remove devices from groups)
- Groups stored in-memory with dict (production would use database)
- **Files changed:**
  - services/device-svc/app/models/group.py (new)
  - services/device-svc/app/models/__init__.py (updated exports)
  - services/device-svc/app/routes/groups.py (new)
  - services/device-svc/app/routes/__init__.py (updated exports)
  - services/device-svc/app/main.py (added groups router)
- **Learnings for future iterations:**
  - Groups use in-memory storage similar to devices - no database integration yet
  - GroupDetail extends DeviceGroup with device_count and online_count computed fields
  - System groups cannot be deleted (type check in delete endpoint)
---

## 2026-04-10 - AUTH-003
- Implemented RBAC middleware for role-based access control (admin, user, viewer roles)
- Created frontend auth store with Zustand + persist middleware for token persistence
- Built login page with gradient background and Ant Design form
- Updated App.tsx with AuthGuard protection and user dropdown menu
- **Files changed:**
  - services/test-svc/app/middleware/rbac.py (new)
  - services/test-svc/app/middleware/__init__.py (updated exports)
  - frontend/src/stores/authStore.ts (new)
  - frontend/src/stores/index.ts (updated exports)
  - frontend/src/pages/auth/Login.tsx (new)
  - frontend/src/pages/auth/Login.css (new)
  - frontend/src/components/AuthGuard/index.tsx (fixed hasPermission import)
  - frontend/src/App.tsx (added auth integration)
  - frontend/src/App.css (added header styles)
- **Learnings for future iterations:**
  - hasPermission is a standalone function, not a store method - import it separately
  - Zustand persist middleware automatically handles localStorage serialization
  - The AuthGuard component was already partially implemented at frontend/src/components/AuthGuard/index.tsx
---

## 2026-04-10 - STATS-002
- Enhanced TrendChart component with histogram chart type for distribution visualization
- Added success_rate metric support to statistics service trend API
- Added response time distribution API endpoint (/api/v1/statistics/response-time-distribution)
- Updated Trend page to show success rate trend and response time distribution charts
- **Files changed:**
  - services/report-svc/app/services/statistics.py (added success_rate metric, response time distribution)
  - services/report-svc/app/api/statistics.py (added response-time-distribution endpoint)
  - frontend/src/components/TrendChart/index.tsx (added histogram type)
  - frontend/src/pages/reports/Trend.tsx (added success rate trend, response time distribution)
- **Learnings for future iterations:**
  - TrendChart component supports 4 chart types: line, bar, area, histogram
  - The histogram type is useful for showing distributions (e.g., response time buckets)
  - Statistics trend API now supports 3 metrics: device_hours, task_count, success_rate
  - Response time distribution returns bucketed data with label, count, and percentage
---

## 2026-04-10 - ALERT-001
- Verified alert notification system implementation
- Alert data model with AlertType, AlertStatus, AlertSeverity, NotificationChannel enums
- Alert rules support device_offline, task_failure_rate, device_idle, custom types
- Alert API routes: CRUD for rules, trigger/acknowledge/resolve alerts, history
- Feishu/DingTalk webhook notifications via httpx async client
- Email notification placeholder (requires SMTP config in production)
- **Files:**
  - services/report-svc/app/models/alert.py (verified)
  - services/report-svc/app/services/alert.py (verified)
  - services/report-svc/app/api/alerts.py (verified)
  - services/report-svc/app/main.py (alerts router registered)
- **Learnings for future iterations:**
  - Alert rules use cooldown_seconds to prevent notification spam
  - Notifications sent via asyncio.create_task for fire-and-forget pattern
  - Feishu uses interactive card format, DingTalk uses markdown format
---

## 2026-04-10 - EXPORT-001
- Created export service for data export functionality
- Export to CSV: generic data, statistics with multiple sections
- Export to Excel: openpyxl with styling, headers, auto-width columns
- Export to PDF: reportlab with tables, summary sections, device usage
- Batch export API for multiple data sets at once
- API endpoints: /export/csv, /export/excel, /export/pdf, /export/statistics/*, /export/batch
- **Files:**
  - services/report-svc/app/services/export.py (new)
  - services/report-svc/app/api/export.py (new)
  - services/report-svc/app/main.py (added export router)
- **Learnings for future iterations:**
  - reportlab and openpyxl are optional dependencies - service handles missing libraries gracefully
  - PDF export uses A4 page size with Table and Paragraph elements
  - Excel uses PatternFill for header styling, auto-adjust column widths
  - Batch export returns base64-encoded content for JSON response
---

## 2026-04-10 - AI-001
- Created AI service (ai-svc) directory structure
- Integrated PaddleOCR (primary) and Tesseract (fallback) OCR engines
- OCR API: /api/v1/ocr/recognize, /api/v1/ocr/recognize/file, /api/v1/ocr/find
- Supports Chinese (ch) and English (en) recognition
- Returns text regions with bounding boxes and confidence scores
- OCR health endpoint to check engine availability
- **Files:**
  - services/ai-svc/app/config.py (new)
  - services/ai-svc/app/services/ocr.py (new)
  - services/ai-svc/app/api/ocr.py (new)
  - services/ai-svc/app/main.py (new)
  - services/ai-svc/requirements.txt (new)
- **Learnings for future iterations:**
  - PaddleOCR is primary engine, Tesseract is fallback
  - OCR engines are lazily initialized on first use
  - TextRegion dataclass contains text, confidence, bbox (4 corners), and center point
  - GPU support via AI_DEVICE setting (cpu or cuda)
---

## 2026-04-10 - AI-002
- Implemented AI element location service
- Natural language query support (e.g., "点击登录按钮")
- Element type detection: button, input, text, icon, link
- Returns element coordinates with bounding box and center point
- Confidence scoring based on OCR confidence and text similarity
- Similar element matching API for finding related UI elements
- **Files:**
  - services/ai-svc/app/services/element_locator.py (new)
  - services/ai-svc/app/api/locate.py (new)
  - services/ai-svc/app/main.py (added locate router)
- **Learnings for future iterations:**
  - Element location uses OCR results combined with keyword matching
  - ElementType enum defines supported element categories
  - Text similarity calculated using character overlap and containment
  - Clickable detection based on element type and keywords
---
