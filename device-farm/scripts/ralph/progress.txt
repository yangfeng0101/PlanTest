# Ralph Progress Log
Started: 2026年 4月10日 星期五 19时05分26秒 CST

## Codebase Patterns
- .env 文件已在 .gitignore 中，不应提交敏感信息
- 环境变量配置文档在 docs/IMPLEMENTATION.md
- 所有服务 DEBUG 默认为 false，通过环境变量控制
- CORS_ORIGINS 从环境变量读取，默认 localhost
- 启动时验证安全配置 (JWT, API_KEY)
- 速率限制使用 Redis，不可用时降级到内存
- 服务端口: device-svc=8001, test-svc=8001, report-svc=8002, ai-svc=8003
- 服务间调用应使用 settings.XXX_SERVICE_URL 配置

---

## 2026-04-10 19:10 - FIX-SEC-001
- 移除 .env 文件中的敏感信息
- 文件变更:
  - device-farm/.env (从版本控制移除)
  - device-farm/.env.example (更新为安全的占位符模板)
  - device-farm/.gitignore (新建)
  - device-farm/docs/IMPLEMENTATION.md (添加环境配置说明)
- **Learnings for future iterations:**
  - .env 文件虽然存在于本地，但已从 git 跟踪中移除
  - 环境变量文档说明已添加到 IMPLEMENTATION.md
  - .env.example 应只包含占位符，不包含真实凭证
---

## 2026-04-10 19:15 - FIX-SEC-002
- DEBUG 模式默认关闭
- 文件变更:
  - services/device-svc/app/config.py (DEBUG 默认值从 True 改为 false)
- **Learnings for future iterations:**
  - test-svc, report-svc, ai-svc 已经正确使用环境变量控制 DEBUG
  - 只有 device-svc 之前硬编码为 True
---

## 2026-04-10 19:20 - FIX-SEC-003
- CORS 配置限制为指定域名
- 文件变更:
  - services/device-svc/app/config.py (CORS 从环境变量读取)
  - services/test-svc/app/config.py, main.py (添加 CORS_ORIGINS 配置)
  - services/report-svc/app/config.py, main.py (添加 CORS_ORIGINS 配置)
  - services/ai-svc/app/config.py, main.py (添加 CORS_ORIGINS 配置)
- **Learnings for future iterations:**
  - 所有服务现在使用 CORS_ORIGINS 环境变量
  - 默认值: http://localhost:3000,http://localhost:5173
  - 生产环境需要设置 CORS_ORIGINS 为实际域名
---

## 2026-04-10 19:25 - FIX-SEC-004
- 设备命令接口添加认证和授权
- 文件变更:
  - services/device-svc/app/routes/devices.py
- **Learnings for future iterations:**
  - device-svc 通过调用 test-svc 验证 JWT token
  - 已有 require_role() 函数可用于角色检查
  - 操作日志使用 logger.info/warning 记录
---

## 2026-04-10 19:30 - FIX-SEC-005
- 认证接口添加速率限制
- 文件变更:
  - services/test-svc/app/middleware/rate_limit.py (新建)
  - services/test-svc/app/main.py (添加中间件)
- **Learnings for future iterations:**
  - 使用 Redis ZSET 实现滑动窗口限流
  - Redis 不可用时自动降级到内存存储
  - 返回标准 Rate Limit 响应头
---

## 2026-04-10 19:35 - FIX-SEC-006
- API Key 启用时强制检查非空
- 文件变更:
  - services/test-svc/app/config.py (添加 validate_api_key_config)
  - services/test-svc/app/main.py (启动时调用验证)
- **Learnings for future iterations:**
  - 配置验证应在启动时进行，避免运行时错误
  - validate_jwt_config 和 validate_api_key_config 模式一致
---

## 2026-04-10 19:40 - FIX-FUNC-001
- 并行执行器集成设备服务 API
- 文件变更:
  - services/test-svc/app/config.py (添加 DEVICE_SERVICE_URL 配置)
  - services/test-svc/app/services/parallel_executor.py (修复 HTTP 调用集成)
- **Learnings for future iterations:**
  - device-svc 运行在端口 8001，test-svc 在 8001，report-svc 在 8002
  - device-svc API 响应格式: {"devices": [...], "total": N}
  - Device ID 字段为 "id"，不是 "device_id"
  - 查询参数 status 应使用 "online" 而非 "available"
  - 应使用 settings.DEVICE_SERVICE_URL 而非手动替换端口号
---

## 2026-04-10 19:45 - FIX-FUNC-002
- 预约通知服务集成
- 文件变更:
  - services/device-svc/app/services/notification_service.py (新建，支持飞书/钉钉/邮件)
  - services/device-svc/app/tasks/reservation_tasks.py (集成通知服务)
- **Learnings for future iterations:**
  - notification_service.py 提供统一的通知发送接口
  - 通过环境变量配置通知渠道: FEISHU_WEBHOOK_URL, DINGTALK_WEBHOOK_URL, SMTP_*
  - 预约提醒默认在开始前5分钟发送
  - 如果没有配置通知渠道，会降级到日志记录
---

## 2026-04-10 19:50 - FIX-FUNC-003
- 前端添加 AI 功能入口
- 文件变更:
  - frontend/src/App.tsx (添加 AI 工具菜单和路由)
  - frontend/src/pages/ai/OCR.tsx (新建，OCR 文字识别页面)
  - frontend/src/pages/ai/Locate.tsx (新建，元素定位页面)
  - frontend/src/pages/ai/Generate.tsx (新建，用例生成页面)
- **Learnings for future iterations:**
  - AI 服务运行在端口 8003
  - OCR API: POST /api/v1/ocr/recognize, /api/v1/ocr/find
  - 元素定位 API: POST /api/v1/locate/locate
  - 用例生成 API: POST /api/v1/generate/generate
  - 菜单支持 children 属性实现子菜单
  - AI 服务 URL 应配置为环境变量
---

## 2026-04-10 19:55 - FIX-FUNC-004
- 前端添加用户管理页面
- 文件变更:
  - frontend/src/App.tsx (添加用户管理菜单和路由，菜单权限过滤)
  - frontend/src/pages/admin/Users.tsx (新建，用户管理页面)
- **Learnings for future iterations:**
  - 后端用户 API 已存在于 services/test-svc/app/api/users.py
  - API: GET/POST /api/v1/users, PATCH /api/v1/users/{id}, DELETE /api/v1/users/{id}
  - 用户角色: admin/user/viewer
  - 用户状态: active/inactive/suspended
  - 菜单项支持 permission 属性进行权限过滤
  - hasPermission 函数可用于检查权限，admin 拥有所有权限
---

## 2026-04-10 20:00 - FIX-FUNC-005
- 前端添加告警配置页面
- 文件变更:
  - frontend/src/App.tsx (添加告警配置菜单和路由)
  - frontend/src/pages/alerts/index.tsx (新建，告警配置页面)
  - frontend/src/stores/authStore.ts (添加 alert 权限定义)
- **Learnings for future iterations:**
  - report-svc 运行在端口 8002，告警 API: /api/v1/alerts/*
  - 告警规则 API: GET/POST /alerts/rules, PUT/DELETE /alerts/rules/{id}
  - 告警实例 API: GET /alerts, POST /alerts/{id}/acknowledge, /alerts/{id}/resolve
  - 告警历史 API: GET /alerts/history/all
  - 页面使用 Tabs 组件分离规则、活动告警、历史三个视图
  - Switch 组件可直接在 Table 列中实现启用/禁用切换
---

## 2026-04-10 20:05 - FIX-FUNC-006
- 告警通知服务完善
- 文件变更:
  - services/report-svc/app/services/notification.py (新建，完整通知服务实现)
  - services/report-svc/app/services/alert.py (集成通知服务，异步通知发送)
  - services/report-svc/app/api/alerts.py (添加通知日志查询接口)
  - services/report-svc/app/config.py (添加 SMTP 配置项)
- **Learnings for future iterations:**
  - notification.py 提供统一的通知发送接口，支持飞书/钉钉/邮件
  - 飞书使用 interactive card 格式，钉钉使用 markdown 格式
  - 邮件发送使用 SMTP，支持 TLS 加密
  - 所有通知发送都有详细日志记录，可通过 API 查询
  - 通知发送使用 asyncio.gather 并行执行，提高效率
  - 环境变量配置: FEISHU_WEBHOOK_URL, DINGTALK_WEBHOOK_URL, SMTP_*, EMAIL_FROM
---

## 2026-04-11 00:30 - FIX-FUNC-007
- Token 存储迁移到 HTTP-only Cookie
- 文件变更:
  - frontend/src/stores/authStore.ts (移除 localStorage 存储，使用 cookie 认证)
  - frontend/src/services/api.ts (添加 withCredentials 和 CSRF token 支持)
  - frontend/src/stores/index.ts (移除 TokenResponse 导出)
  - frontend/src/pages/admin/Users.tsx (更新 fetchWithAuth 使用 cookie)
  - frontend/src/pages/alerts/index.tsx (更新 fetchWithAuth 使用 cookie)
  - services/test-svc/app/api/auth.py (添加 CSRF token 验证依赖)
- **Learnings for future iterations:**
  - HTTP-only Cookie 比 localStorage 更安全，可防止 XSS 攻击
  - CSRF token 存储在非 HTTP-only cookie 中，前端可读取并在请求头中发送
  - 前端请求需要设置 credentials: 'include' 才能发送 cookie
  - axios 使用 withCredentials: true 配置
  - Headers 对象用于设置自定义 header，避免 TypeScript 类型问题
  - 后端已实现 validate_csrf_token 和 require_csrf_token 依赖
  - Cookie 设置通过环境变量控制: COOKIE_SECURE, COOKIE_SAMESITE
---
