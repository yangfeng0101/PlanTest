## Codebase Patterns

- Go services use internal/ for private packages
- Python services use FastAPI with async/await
- Frontend uses React with TypeScript and Vite
- WebRTC for real-time video streaming
- MinIO for object storage with async executor
- Safe Python execution with restricted builtins
- Safe JavaScript execution with Node.js VM sandbox

---

## 2026-04-10 - Ralph Setup

- Created prd.json with Phase 2 user stories
- Installed Ralph scripts to scripts/ralph/
- Installed Ralph skills globally (~/.claude/skills/)

**Learnings for future iterations:**
- Phase 1 defects DEF-001 through DEF-017 have been addressed
- Critical security issues (code injection) fixed with sandboxing
- WebRTC improvements needed (reconnect, FPS, memory leaks)
- iOS and Harmony device support is the next major work

---

## 2026-04-10 - DEF-018

- Implemented API authentication middleware
- Created AuthService for API key management
- Added AuthMiddleware for request-level authentication
- Protected all API endpoints with verify_api_key dependency
- Support for both X-API-Key header and Bearer token

Files changed:
- services/test-svc/app/services/auth.py (new)
- services/test-svc/app/middleware/auth.py (new)
- services/test-svc/app/middleware/__init__.py (new)
- services/test-svc/app/api/scripts.py (modified - added auth to all endpoints)
- services/test-svc/app/main.py (modified - added AuthMiddleware)
- services/test-svc/app/config.py (modified - API_KEY_ENABLED defaults to true)

**Learnings for future iterations:**
- FastAPI middleware is applied in reverse order (last added runs first)
- Public paths must be explicitly listed in AuthMiddleware.PUBLIC_PATHS
- API key validation uses dependency injection pattern with Depends()
- Both middleware and dependency injection provide redundant protection

---

## 2026-04-10 - DEF-011

- Added context.Context to ScreenSession struct for proper goroutine lifecycle management
- Goroutine leak fix already partially implemented: cancel function stored and streamVideo responds to context.Done()
- StartSession uses context.WithCancel as expected
- StopSession calls cancel() to terminate the streamVideo goroutine

Files changed:
- services/screen-svc/internal/handler/manager.go (added ctx field to struct)

**Learnings for future iterations:**
- Go context pattern: store both ctx and cancel in struct for goroutine lifecycle control
- The select/case pattern with ctx.Done() is the idiomatic way to make goroutines cancellable
- Always cancel context before cleaning up resources to stop dependent goroutines

---
## 2026-04-10 - DEF-012

- Fixed useEffect dependency array to [deviceId, wsUrl] to prevent connection churn from callback function dependencies
- Ensured statsInterval is properly cleared before creating new one and ref is reset to null in cleanup
- Verified reconnectTimer is cleared in stopConnection and on successful connection
- stopConnection is called via useEffect cleanup function on component unmount
- Fixed pre-existing TypeScript errors (longPressDelay typo in TouchHandler, unused Switch import in screen page)

Files changed:
- frontend/src/components/WebrtcPlayer/index.tsx (memory leak fix)
- frontend/src/components/TouchHandler/index.tsx (typo fix)
- frontend/src/pages/screen/index.tsx (unused import fix)

**Learnings for future iterations:**
- React useEffect with callback functions in dependencies causes unnecessary re-runs; use refs to store callbacks instead
- Always clear intervals/timeouts before creating new ones in useEffect to prevent multiple timers
- TypeScript strict mode catches pre-existing issues that should be fixed for clean builds
- The ref pattern (useRef + useEffect to sync) avoids stale closures without triggering re-renders

---

## 2026-04-10 - DEF-013

- Verified FPS calculation already uses WebRTC getStats() API correctly
- Reads framesPerSecond from inbound-rtp report (Chrome 78+)
- Fallback uses framesDecoded delta calculation for browsers without framesPerSecond
- No invalid byte-based estimation logic present

Files changed:
- None (already correctly implemented)

**Learnings for future iterations:**
- The DEFECTS.md described a byte-based FPS estimation `Math.round(bytesReceived / 10000)` but this was already fixed in the initial commit
- Always verify current code state against defect descriptions before making changes
- WebRTC getStats() provides framesPerSecond directly for modern browsers
- Fallback using framesDecoded delta is a reliable alternative for older browsers

---

## 2026-04-10 - DEF-014

- Added MAX_RECONNECT_DELAY constant (30000ms) to cap exponential backoff
- Implemented Math.min() to ensure delay never exceeds 30 seconds
- Added reconnection handling for 'disconnected' state (in addition to 'failed')
- Reconnection now properly stops old connection before starting new one on 'disconnected' state

Files changed:
- frontend/src/components/WebrtcPlayer/index.tsx (reconnection improvements)

**Learnings for future iterations:**
- WebRTC connections can enter 'disconnected' state temporarily before 'failed' - handling both provides better resilience
- Exponential backoff must always have a maximum cap to prevent excessive delays
- When reconnecting from 'disconnected' state, must fully stop old connection first to clean up resources

---

## 2026-04-10 - DEF-015

- Verified touch coordinate calculation is already correctly implemented
- Uses containerRef to get container bounds (not e.target)
- Correctly calculates relative coordinates (clientX - rect.left, clientY - rect.top)
- Properly scales to device coordinates (x/width * screenWidth)

Files changed:
- None (already correctly implemented)

**Learnings for future iterations:**
- The TouchOverlay uses the ref pattern (setContainerRef callback) to get accurate container bounds
- Using e.target for getBoundingClientRect() can return wrong bounds from child elements
- Coordinate mapping: relativeX * (screenWidth / containerWidth) scales properly for any container size

---

## 2026-04-10 - DEF-016

- Verified long-press timer cleanup is already correctly implemented
- useEffect returns cleanup function that clears longPressTimerRef.current
- Timer is also cleared on pointerUp and when swipe is detected

Files changed:
- None (already correctly implemented)

**Learnings for future iterations:**
- Timer refs should always be cleared in useEffect cleanup to prevent memory leaks on unmount
- Multiple cleanup points (unmount, pointerUp, swipe detection) ensure timers don't leak
- The pattern `useRef<number | null>(null)` with `window.setTimeout` and `clearTimeout` is idiomatic for React timers

---

## 2026-04-10 - IOS-001

- Created ios_service.py with IOSDeviceService class
- Implemented discover_devices() using pymobiledevice3 usbmux list
- Implemented get_device_info(udid) via lockdown info query
- Implemented pair_device(udid) with support for handling user confirmation requirement
- Added pymobiledevice3 dependency to requirements.txt
- Bonus: Implemented unpair_device, get_pairing_status, list_apps, install_app, uninstall_app

Files changed:
- services/device-svc/app/services/ios_service.py (new)
- services/device-svc/requirements.txt (modified - added pymobiledevice3)

**Learnings for future iterations:**
- pymobiledevice3 CLI is invoked via `python3 -m pymobiledevice3` with JSON output via `--json` flag
- iOS device identification uses UDID (Unique Device Identifier) not serial number
- Pairing may require user confirmation on the device screen (PasswordRequired/UserPairingRequired errors)
- Device info is retrieved via lockdown service; battery and storage require separate domain queries
- Screen resolution/size for iOS devices must be mapped from ProductType as lockdown doesn't expose display info directly
- The ADBService pattern (async subprocess execution with stdout/stderr handling) works well for pymobiledevice3 too

---
