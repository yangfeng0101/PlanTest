# Ralph Progress Log
Started: 2026年 4月13日 星期一 15时08分39秒 CST
---

## 2026/04/13 15:10 - SEC-001
- Verified AuthMiddleware already implemented in ai-svc
- The middleware validates JWT tokens via test-svc /api/v1/auth/me endpoint
- All API endpoints protected (only /, /health, docs are public)
- Files reviewed: services/ai-svc/app/main.py, services/ai-svc/app/middleware/auth.py
- **Learnings for future iterations:**
  - ai-svc uses BaseHTTPMiddleware pattern for auth (not route-level Depends)
  - Token validation delegates to test-svc, avoiding JWT secret duplication
  - PUBLIC_PATHS in middleware controls which paths bypass auth
---

## 2026/04/13 15:15 - SEC-002
- Added authentication to all alert API endpoints in report-svc
- Admin-only operations: create/update/delete/enable/disable rules
- Regular authenticated users can: list/view alerts, acknowledge/resolve
- User ID for acknowledge/resolve now comes from token (not request body)
- Files changed:
  - services/report-svc/app/api/alerts.py - Added Depends(get_current_user) and Depends(require_role("admin"))
  - services/report-svc/app/middleware/auth.py - Updated require_role for case-insensitive comparison
- **Learnings for future iterations:**
  - report-svc middleware uses same pattern as ai-svc (validates via test-svc HTTP call)
  - require_role accepts string role names ("admin", "user", "viewer")
  - get_current_user_id is useful for operations that need user ID from token
---

## 2026/04/13 15:25 - DATA-001
- Created DeviceDB SQLAlchemy model for device persistence
- Implemented DeviceDatabaseService with full CRUD operations
- Modified DeviceService to persist all device changes to database
- Added database initialization on service startup
- Files changed:
  - services/device-svc/app/models/device_db.py - New DeviceDB model
  - services/device-svc/app/services/device_db_service.py - New persistence service
  - services/device-svc/app/services/device_service.py - Added database integration
  - services/device-svc/app/main.py - Added database init on startup
- **Learnings for future iterations:**
  - DeviceService keeps in-memory cache for performance, syncs to DB on changes
  - On startup, devices are loaded from DB first, then ADB scan updates them
  - Used SQLAlchemy merge() for upsert operations (insert or update)
  - DeviceDB uses JSON column for tags (could be optimized with junction table)
  - Status changes (occupy/release) are persisted immediately to DB
---

## 2026/04/13 15:40 - DATA-002
- Created AlertRuleDB, AlertDB, AlertHistoryDB SQLAlchemy models
- Created report-svc database.py module for connection management
- Implemented AlertDatabaseService with full CRUD operations
- Modified AlertService to use in-memory cache + async DB persistence
- Added database initialization on service startup
- Files changed:
  - services/report-svc/app/database.py - New database module
  - services/report-svc/app/models/alert_db.py - New Alert DB models
  - services/report-svc/app/services/alert_db_service.py - New persistence service
  - services/report-svc/app/services/alert.py - Added database integration
  - services/report-svc/app/main.py - Added database init and lifespan
- **Learnings for future iterations:**
  - AlertService uses async background tasks for DB writes (fire-and-forget pattern)
  - Rules/alerts loaded from DB on startup, then cached in memory
  - Alert history is purely in database (no memory cache needed)
  - JSON columns used for channels, recipients, and details fields
  - Notification count increment is a separate DB operation
---

## 2026/04/13 15:50 - SEC-003
- Removed default credentials display from Login.tsx
- The login page previously showed "默认管理员账号: admin / admin123" hint
- Also removed unused Space import from antd
- Files changed:
  - frontend/src/pages/auth/Login.tsx - Removed credentials hint and unused import
- **Learnings for future iterations:**
  - Never display default credentials in production UI
  - Run `npx tsc --noEmit` in frontend to check for TypeScript errors
  - Need to run `npm install` first before type checks if node_modules missing
---

## 2026/04/13 16:00 - SEC-004
- Added RateLimitMiddleware to device-svc for command endpoint rate limiting
- Limits POST requests to /devices/{id}/command to 60 requests per minute per client IP
- Returns 429 Too Many Requests when limit exceeded
- Includes standard rate limit headers (Retry-After, X-RateLimit-Limit, X-RateLimit-Remaining, X-RateLimit-Reset)
- Files changed:
  - services/device-svc/app/middleware/rate_limit.py - New rate limit middleware
  - services/device-svc/app/middleware/__init__.py - Export RateLimitMiddleware
  - services/device-svc/app/main.py - Add middleware to app
- **Learnings for future iterations:**
  - Rate limiting uses in-memory storage (suitable for single instance)
  - Only applies to POST requests with "/command" in path
  - Middleware added after CORS middleware in the stack
  - For distributed deployments, consider Redis-based rate limiting like test-svc
---
