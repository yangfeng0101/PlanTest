# Ralph Progress Log
Started: 2026年 4月13日 星期一 15时08分39秒 CST
---

## 2026/04/13 15:10 - SEC-001
- Verified AuthMiddleware already implemented in ai-svc
- The middleware validates JWT tokens via test-svc /api/v1/auth/me endpoint
- All API endpoints protected (only /, /health, docs are public)
- Files reviewed: services/ai-svc/app/main.py, services/ai-svc/app/middleware/auth.py
- **Learnings for future iterations:**
  - ai-svc uses BaseHTTPMiddleware pattern for auth (not route-level Depends)
  - Token validation delegates to test-svc, avoiding JWT secret duplication
  - PUBLIC_PATHS in middleware controls which paths bypass auth
---

## 2026/04/13 15:15 - SEC-002
- Added authentication to all alert API endpoints in report-svc
- Admin-only operations: create/update/delete/enable/disable rules
- Regular authenticated users can: list/view alerts, acknowledge/resolve
- User ID for acknowledge/resolve now comes from token (not request body)
- Files changed:
  - services/report-svc/app/api/alerts.py - Added Depends(get_current_user) and Depends(require_role("admin"))
  - services/report-svc/app/middleware/auth.py - Updated require_role for case-insensitive comparison
- **Learnings for future iterations:**
  - report-svc middleware uses same pattern as ai-svc (validates via test-svc HTTP call)
  - require_role accepts string role names ("admin", "user", "viewer")
  - get_current_user_id is useful for operations that need user ID from token
---
