# GEMINI.md - Device Farm 项目上下文与约束

> Codex/通用编码 Agent 请优先读取根目录 `AGENTS.md`；本文件保留给 Gemini 使用。

## 1. 项目概览
- **Device Farm** 是一个企业级移动设备管理与自动化测试平台。
- **架构**: 采用 Python (FastAPI) 微服务集群，Go (Pion WebRTC) 处理屏幕流，Celery 调度任务。

## 2. 开发者操作约束 (代码修复阶段)

### 一、修改原则（核心）
1. **最小改动原则**
   - 能用 ≤5 行解决的问题，禁止写 100 行。
   - 禁止无关重构。
   - 禁止“顺手优化”非问题代码。
2. **有理有据原则**
   - 每一次修改必须说明：
     - 问题根因（Root Cause）
     - 为什么这样改可以解决问题
   - 禁止：盲改、猜测式修改、多方案试错。
3. **不改变实现方案**
   - 必须基于原有架构修复。
   - 禁止重写系统。
   - 禁止替换核心技术（除非用户明确要求）。

### 二、修改流程（必须按顺序）
1. **Step 1: 问题定位**
   - 必须明确：出错位置（文件 + 行为）、根本原因（不是表象）。
2. **Step 2: 修复方案**
   - 给出最小改动方案。
   - 标明修改文件。
3. **Step 3: 代码修改（必须）**
   - 要求：只展示修改相关代码，用清晰方式标注（如 before / after 或 diff）。
4. **Step 4: 影响分析**
   - 必须说明：是否影响其他模块、是否引入副作用。
5. **Step 5: 端到端验证（强制）**
   - 必须提供完整测试方案：启动测试（服务是否正常启动）、核心功能测试（修复的问题是否解决）、回归测试（原有功能是否正常）、边界测试（如适用，异常输入/网络情况）。

### 三、代码约束
- 保持原有代码风格。
- 保持接口不变（除非必要）。
- 保持兼容性。

### 四、禁止行为（重点）
❌ 大规模重写
❌ 引入新架构
❌ 修改无关代码
❌ 不解释原因直接改代码
❌ 只给修改不做验证
❌ 多次试错式修改

### 五、输出格式（固定）
# Step 1: 问题定位
...
# Step 2: 修复方案
...
# Step 3: 代码修改
...
# Step 4: 影响分析
...
# Step 5: 验证方案
...

### 六、成功标准
你的修改必须满足：
✅ 改动最小
✅ 问题被彻底解决
✅ 无副作用
✅ 所有测试通过

## 3. 关键入口索引
- 项目入口: `device-farm/README.md`
- 容器编排: `device-farm/infra/docker/docker-compose.yml`
- 投屏核心: `device-farm/services/screen-svc/`
- 测试引擎: `device-farm/services/test-svc/`
